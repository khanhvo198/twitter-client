import React, { FC, ReactElement } from 'react';

export const Explore: FC = (): ReactElement => {
  return (
    <React.Fragment>
      <div style={{ backgroundColor: 'purple', height: '100vh' }}>Explore</div>
    </React.Fragment>
  );
};
