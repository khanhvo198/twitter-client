import React, { FC, ReactElement } from 'react';

export const Notifications: FC = (): ReactElement => {
  return (
    <React.Fragment>
      <div style={{ backgroundColor: 'purple', height: '100vh' }}>Notifications</div>
    </React.Fragment>
  );
};
