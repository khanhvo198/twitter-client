import React, { FC, ReactElement } from 'react';

export const Profile: FC = (): ReactElement => {
  return (
    <React.Fragment>
      <div style={{ backgroundColor: 'purple', height: '100vh' }}>Profile</div>
    </React.Fragment>
  );
};
