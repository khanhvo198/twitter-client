import React, { FC, ReactElement } from 'react';

export const Messages: FC = (): ReactElement => {
  return (
    <React.Fragment>
      <div style={{ backgroundColor: 'purple', height: '100vh' }}>Messages</div>
    </React.Fragment>
  );
};
